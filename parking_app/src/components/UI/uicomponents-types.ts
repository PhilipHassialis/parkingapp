export type User = {
    name: string;
    role: string;
    image: string;
}

export type ApplicationCardContent = {
    title: string;
    content: string;
    relativeUrl: string;
    bgColour: string;
}